/**
 * LLM Provider enum
 * @enum {string}
 */
export const LLMProvider = {
  BEDROCK: 'bedrock',
  OLLAMA: 'ollama'
};

// Note: All type definitions are maintained in the types.d.ts file
// This file only contains runtime values like enums that need to be available at runtime
